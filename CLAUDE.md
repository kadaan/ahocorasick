# nexus-context-resolver Project Guidelines

## Project Conventions
- **Always use devbox to run Go commands**: All Go commands must be executed via devbox, e.g., `devbox run go test`, `devbox run go build`, `devbox run go run`, etc.
